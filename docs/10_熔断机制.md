# 多模型动态降级熔断机制

## 触发条件
当全链路追踪（Trace 树）监测到主模型（如 GPT-4o / Claude 3.5 Sonnet）的==报错率超过 50% 或延迟飙升至 10 秒以上==时，图的条件路由必须秒级触发断路器（Circuit Breaker）。

## 降级策略

触发后，图的条件路由自动所有后续节点的 LLM 客户端切换至本地私有化部署的开源大模型,如：

- Llama-3.1-70B
- Qwen-2.5-72B

## 权衡

- 模型智商可能略微下降
- 但核心业务流水线**永不断流**
